{
  "name": "ai4s-dashboard",
  "private": true,
  "engines": { "node": ">=18" },
  "scripts": {
    "build": "node build.js",
    "mock": "node build.js --mock",
    "test": "node --test --test-concurrency=1"
  }
}
