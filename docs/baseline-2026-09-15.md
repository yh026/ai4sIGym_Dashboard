# 2026-09-15：Key Findings 改造前基线

## 正式站基线

2026-09-15 通过正式站回执和 GitHub 分支读取确认：

| 项目 | 值 |
| --- | --- |
| 正式站 commit | `1626fb1f55391c8c1ba6a91e5a199645d3f3beab` |
| 正式部署 ID | `6aa78719ff58fc00081400cb` |
| 构建时间（UTC） | `2026-09-14T05:35:24.723Z` |
| Registry revision | `sha256:65307e13063379158ecfe9c07837f34b5091dbcc64671aa6595fe823f67588b2` |
| 清单 | schema 2 / taxonomy 6，11 个项目 |
| 本地正式站标签 | `baseline/production-2026-09-15` |

证据：[正式部署回执](https://aisigym.netlify.app/deploy-receipt.json)、
[正式清单](https://aisigym.netlify.app/manifest.json)。这些在线地址会随发布变化；
检查时的 JSON 已随本地审查备份保存。

清理前 `develop@ba6ed18f8f5b51477dd0b34ff17eac16e0cddea9` 与上述正式 commit
的 Git tree 一致。网站的构建版本与线上 Apps Script 部署是两件事；此次没有读取或
变更线上脚本和表格，不能将当前 `Code.gs` 当作已验证的线上脚本版本。

## 本地开发基线

标签：`baseline/pre-key-findings-2026-09-15`。

原工作目录包含 60 个已跟踪修改和 297 个未跟踪文件。先在仓库外备份 Git 历史、
357 个原始文件、完整 patch、逐文件 hash 及正式部署回执，再整理为两个本地提交：

1. **Drive 重新上传同步修复**（`faed88b`）。保护已有目录身份，拒绝不安全替换后的
   重复项目创建；维护同一轮扫描的目录归属；统一 Apps Script / Node 的可选内部
   `source_folder_id` 合同，保留旧表兼容性和公开字段边界。
2. **Payload dashboard 工具与测试**。保存生成器、schema、CLI、文档、示例 payload
   及必要数据；恢复技能目录链接，让测试直接引用工具目录；移除个人 CSV 路径默认值。

60 处执行权限变化已恢复。ZIP、交付测试包、临时两行数据和生成 HTML 共 100 个
文件移到仓库外存档。TESS 大 HTML 的回归检查改为从源输入临时生成，并与小型
`expected-build.json` 中保留的原始 payload / bundle hash 比较。

## 验证

- Node.js v24.19.0。
- 新回归用例先确认旧逻辑失败，再验证修复通过。
- 同步提交的已跟踪测试：374 / 374 通过。
- 包含生成工具的完整测试：454 / 454 通过。
- Python 示例源码通过语法检查；科学分析未重新运行，示例科学数据保持原样。
- 该记录与标签只保存本地基线。本次没有推送、合并或部署网站，也没有修改 Google Sheet
  或部署 Apps Script；提交信息包含 `[skip netlify]`。

后续页面结构改造可从本地开发基线创建功能分支。正式发布同步修复前，仍须按现有
Preview 流程核对线上表格与 Apps Script，再单独安排上线。
